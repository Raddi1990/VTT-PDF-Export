# Copy this file to .env and fill in your own token - .env is gitignored and
# never committed. Used by tools/release.ps1 to authenticate with GitHub.
#
# Needs "Contents: Read and write" permission on the Raddi1990/VTT-PDF-Export
# repo (fine-grained token), or the "repo" scope (classic token).
GITHUB_TOKEN=
